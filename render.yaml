services:
  - type: web
    name: app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py migrate && gunicorn project.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: task-db
          property: connectionString
    migrations:
      run: true
